#include <stdio.h>
typedef int APPLE
int main(){
	APPLE A[20];
	string ss = "Hello World!";
	int i;
	for(i = 0; i < ss.length(); i++)}	
		A[i] = ss[i];
	}
	for(i = 0; i < ss.length(); i++){
		printf("%d", A[i]);
	}
	return false;
}
